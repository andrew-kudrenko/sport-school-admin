import React from 'react'
import { TicketsEditorLayout } from '../../components/tickets/TicketsEditorLayout'

export const EditTicketView: React.FC = () =>
    <TicketsEditorLayout title="Редактировать абонемент" mode="edit" />